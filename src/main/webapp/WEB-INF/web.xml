<?xml version="1.0" encoding="UTF-8"?>

<web-app
    xmlns="https://jakarta.ee/xml/ns/jakartaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        https://jakarta.ee/xml/ns/jakartaee
        https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
    version="6.0">

    <display-name>LawSuit Tracker</display-name>

    <!-- ===================== -->
    <!--        FILTERS        -->
    <!-- ===================== -->

    <filter>
        <filter-name>AuthenticationFilter1</filter-name>
        <filter-class>com.lawsuittracker.util.RestAuthenticationFilter</filter-class>
    </filter>

    <filter>
        <filter-name>AuthenticationFilter2</filter-name>
        <filter-class>com.lawsuittracker.util.PVAuthenticationFilter</filter-class>
    </filter>

    <filter>
        <filter-name>CORSFilter2</filter-name>
        <filter-class>com.lawsuittracker.util.FilterAction</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>AuthenticationFilter1</filter-name>
        <url-pattern>/rest/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>AuthenticationFilter2</filter-name>
        <url-pattern>/pvApi/*</url-pattern>
    </filter-mapping>

    <filter-mapping>
        <filter-name>CORSFilter2</filter-name>
        <url-pattern>/rest/*</url-pattern>
    </filter-mapping>

    <!-- ===================== -->
    <!--       LISTENER        -->
    <!-- ===================== -->

    <listener>
        <listener-class>
            org.quartz.ee.servlet.QuartzInitializerListener
        </listener-class>
    </listener>

    <!-- ===================== -->
    <!--        JERSEY          -->
    <!-- ===================== -->

    <servlet>
        <servlet-name>jersey-rest</servlet-name>
        <servlet-class>
            org.glassfish.jersey.servlet.ServletContainer
        </servlet-class>

        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>
                com.lawsuittracker.service,
                com.lawsuittracker.pvIntService
            </param-value>
        </init-param>

        <init-param>
            <param-name>jersey.config.server.provider.classnames</param-name>
            <param-value>
                org.glassfish.jersey.filter.LoggingFilter,
                org.glassfish.jersey.media.multipart.MultiPartFeature
            </param-value>
        </init-param>

        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>jersey-rest</servlet-name>
        <url-pattern>/rest/*</url-pattern>
    </servlet-mapping>

    <servlet-mapping>
        <servlet-name>jersey-rest</servlet-name>
        <url-pattern>/pvApi/*</url-pattern>
    </servlet-mapping>

    <!-- ===================== -->
    <!--     SESSION CONFIG    -->
    <!-- ===================== -->

    <session-config>
        <session-timeout>480</session-timeout>
    </session-config>

    <!-- ===================== -->
    <!--    DATABASE JNDI      -->
    <!-- ===================== -->

    <resource-ref>
        <description>Primary SQL Server Datasource</description>
        <res-ref-name>jdbc/RELCALDB</res-ref-name>
        <!-- res-ref-name>jdbc/RCBRPL-95002539/SQLEXPRESS</res-ref-name> -->
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <resource-ref>
        <description>Secondary SQL Server Datasource</description>
        <res-ref-name>jdbc/RCBRPL-95002539/SQLEXPRESS_SECONDDB</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

</web-app>
